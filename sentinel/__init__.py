# -*- coding: utf-8 -*-
"""sentinel — живой дозорный: значимое движение на Variational + подтверждение смарт-деньгами.

ЧТО ЭТО И ЧЕМ ОТЛИЧАЕТСЯ ОТ ОСТАЛЬНЫХ РАССЫЛЬЩИКОВ БОТА
Все существующие алерты бота ждут расписания (`oc_alerts`, `perp_watch`, `tw_watch` тикают
джобой) и почти все спрашивают у LLM перед отправкой. Дозорный устроен иначе по ДВУМ
пунктам, и оба — про то, что человек делает с алертом РУКАМИ на omni.variational.io:

1. СНАЧАЛА ЧИСЛО, ПОТОМ СЛОВА. Карточка собирается детерминированно (`cards.py`) из наших
   же снимков и уходит немедленно. Обогащение (Нансен + твиттер + пересказ моделью) —
   ВТОРОЕ сообщение следом (`enrichment.py`), и его провал не отменяет и не задерживает
   первое. В `perp_watch` сделано наоборот — LLM внутри пути отправки с таймаутом 12с, то
   есть человек ждёт модель, чтобы узнать цену.
2. LLM НЕ РЕШАЕТ НИЧЕГО. Ни дедупликацию, ни силу события, ни кому отправлять, ни «доставлено».
   Модель умеет ровно одно: пересказать то, что уже посчитано и уже отправлено.

ПОЧЕМУ МОДУЛЬ ЛЕЖИТ ОТДЕЛЬНЫМ ПАКЕТОМ, А НЕ ФУНКЦИЕЙ В `bot.py`
Опрос идёт раз в 15 секунд. Планировщик бота делит цикл с Telethon, PTB и несколькими
десятками джоб, и пропуски там уже случались (сторож `jobs_registry` для этого и заведён), а
деплой перезапускает службу целиком — то есть кольцевой буфер котировок начинал бы жизнь
заново при каждом `git pull`. Поэтому домен (`feed`/`detector`/`store`/`cards`) НЕ знает, кто
его тикает: сегодня это `add_svc` в боте, завтра — отдельный юнит `sentinel.main`, и перенос
не трогает ни одной строки логики. Захват `store.lease()` не даёт двум процессам опрашивать
площадку одновременно, когда оба окажутся живы в момент переезда.

ГРАНИЦА С ТОРГОВЫМ КОНТУРОМ — ЖЁСТКАЯ И ПРОВЕРЯЕТСЯ ТЕСТОМ
Ни один файл пакета не импортирует `nansen_signer` и не зовёт торговые маршруты
(quote/prepare/execute). Дозорный только СМОТРИТ и рассказывает; заходит в позицию человек
руками на сайте площадки. Инвариант держит тест графа импортов (`t_sentinel_*`).

ЧТО ЧИТАЕМ У ПЛОЩАДКИ: ОДНУ ПУБЛИЧНУЮ РУЧКУ
`GET /metadata/stats` у omni-client-api отдаёт ВСЕ 553 инструмента одним ответом: цена,
объём, открытый интерес по сторонам, фандинг, спред и котировки на объём (1k/100k/1m) с
отметкой свежести. Один запрос на весь рынок — поэтому опрос дешёвый и не требует ни ключа,
ни подписи. Недокументированные ручки веб-приложения не трогаем: одна из них
(`/api/metadata/supported_assets`) отвечает 403 через Cloudflare, и строить дозор на том,
что сегодня отвечает, — это алерт, который однажды молча замолчит.
"""

#: МОМЕНТ ИМПОРТА ПАКЕТА - РАНЬШЕ ЛЮБОГО ЕГО МОДУЛЯ (этап 3). По нему `engine.code_stale` узнаёт,
#: что файлы на диске новее процесса: модули пакета импортируются лениво и в разное время, а
#: пакет - всегда первым, значит это самое раннее время, с которого процесс держит код в памяти.
import time as _time  # noqa: E402
IMPORTED_AT = _time.time()
