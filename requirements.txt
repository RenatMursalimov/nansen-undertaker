# ЧТО НУЖНО, И ЧТО НУЖНО НЕ ВСЕГДА.
#
# Обязателен один пакет. Остальные - по фактической нужде, и без них слой не падает, а
# честно говорит, чего не может: «картинки недоступны, текст работает». Список из десяти
# строк «на всякий случай» - это способ не запуститься у того, кто пришёл посмотреть.

httpx>=0.24            # единственный обязательный: провод к api.nansen.ai

# ── дальше необязательное ──
matplotlib>=3.5        # команды png-* (потоки и вероятность рынка картинкой)
Pillow>=10.0           # повторная сборка social preview 1280×640
python-dotenv>=1.0     # читать ключ из .env, а не из окружения
